{
  "name": "valid-impression-pixels-check",
  "version": "1.0.0",
  "description": "a script that will programmatically check whether all impressions pixels are valid.",
  "main": "index.js",
  "scripts": {
    "executePixelValidation": "node -e 'require(\"./index\").executePixelValidation()'",
    "test": "mocha"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csv-parser": "^3.0.0",
    "mocha": "^8.3.2",
    "node-fetch": "^2.6.1"
  }
}
